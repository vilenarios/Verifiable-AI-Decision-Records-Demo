# Test/dev-only dependencies. Kept separate from `requirements.txt`
# because the Dockerfile (Railway prod image) installs the runtime
# file directly — bundling pytest into prod is unnecessary mass.
#
# Install locally with:  pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
pytest-xdist>=3.0
