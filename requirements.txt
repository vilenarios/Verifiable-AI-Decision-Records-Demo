# MLflow plugin — provides ario_mlflow.{anchor, ArioMlflowClient, VerifiedModel}
# and pulls in its own deps (mlflow, PyNaCl, turbo-sdk, requests, jcs, cryptography).
ar-io-mlflow>=0.1.0

# Demo app dependencies (FastAPI + Jinja2 + UI + tracing)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
Jinja2>=3.1.0
pydantic>=2.5.0

# scikit-learn is used by the demo's training script + tests
scikit-learn>=1.3.0

# OpenTelemetry — demo wires up trace export; the plugin auto-captures
# trace IDs from active spans into anchored payloads when present.
opentelemetry-api>=1.21.0
opentelemetry-sdk>=1.21.0
opentelemetry-instrumentation-fastapi>=0.42b0
